apiVersion: apps/v1
kind: Deployment
metadata:
  name: url-lookup
  namespace: production
  labels:
    app: url-lookup
    environment: production
spec:
  replicas: 5
  selector:
    matchLabels:
      app: url-lookup
  template:
    metadata:
      labels:
        app: url-lookup
        environment: production
    spec:
      imagePullSecrets:
      - name: dockerhub-secret
      terminationGracePeriodSeconds: 10
      containers:
      - name: url-lookup
        image: meigec/url-lookup-service:production_placeholder
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        env:
        - name: REDIS_HOST
          value: "redis-service.production.svc.cluster.local"
        - name: REDIS_PORT
          value: "6379"
        - name: ENVIRONMENT
          value: "production"
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: url-lookup-service
  namespace: production
spec:
  selector:
    app: url-lookup
  ports:
  - port: 80
    targetPort: 8080
  type: LoadBalancer